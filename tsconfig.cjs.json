{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "dist/cjs" /* Specify an output folder for all emitted files. */,
    "declarationDir": "dist/cjs/types" /* Specify the output directory for generated declaration files. */
  }
}
